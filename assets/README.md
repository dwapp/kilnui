# Assets

Place a TTF font file here named `Inter-Regular.ttf`.

You can download it from: https://github.com/rsms/inter/releases

The demo expects the file at `assets/Inter-Regular.ttf` relative to the executable.
Any TTF font will work — just rename it to `Inter-Regular.ttf` or update the path in `demo/main.c`.
